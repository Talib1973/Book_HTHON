# Auth Service URL (Better Auth)
# Used at build time via customFields in docusaurus.config.ts
AUTH_SERVICE_URL=http://localhost:3001

# API Backend URL (FastAPI)
# Used at build time via customFields in docusaurus.config.ts
BACKEND_API_URL=http://localhost:8000
